const config = {
   guildId: '',

   registerChannelId: '',
   verifyLogChannelId: '',

   verifyRoleId: '',

   youtubeAlerts: {
      // format ytChannelID: discordChannelID
   },
};

export default config;
