const config = {
   guildId: '',

   registerChannelId: '9',
   verifyLogChannelId: '',

   verifyRoleId: '',

   youtubeAlerts: {
      // format ytChannelID: discordChannelID
   },
};

export default config;
